# Biomedical Sound Separation Using Generative Models

A research-grade ML pipeline to separate heart and lung sounds
from mixed stethoscope recordings using generative models (VAE / CVAE / Diffusion).

## Setup

```bash
python -m venv venv
venv\Scripts\activate        # Windows
source venv/bin/activate     # Mac/Linux

pip install -r requirements.txts